network:
  version: 2
  renderer: networkd
  ethernets:
    ens160:
      dhcp4: no
      addresses:
        - "{{ ansible_host }}/24"
      gateway4: "{{ gateway }}"
      nameservers:
        addresses:
        {% for address in dns_servers %}
          - {{ address }}
        {% endfor %}